import { Inter, Maven_Pro } from "next/font/google";


const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
});

const maven = Maven_Pro({
    subsets: ['latin'],
    display: 'swap',
});

export {inter, maven}